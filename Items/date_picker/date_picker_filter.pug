extends ../widget/widget.pug

include date_picker_mixin.pug

prepend header
  div.date_picker__caption_justifyer

block caption
  h3 Прибытие

block rcaption
  h3 Выезд

block content
  div(onchange=onchange).date_picker__filter
    +date_picker(true, null, "date_picker_filter__selected_from(this)")
    +date_picker(true, true, "date_picker_filter__selected_to(this)")
  - onchange = null
      